# Encord SDK + Agents
encord>=0.1.130
encord-active>=0.1.74
encord-agents[vision]>=0.1.0

# Inference client (calls vLLM on the GH200 — model not loaded locally)
openai>=1.30.0

# Vision utilities (frame encoding for vLLM requests)
Pillow>=9.4.0
numpy>=1.24.4
opencv-python>=4.8.0

# General
requests>=2.28.0
python-dotenv>=1.0.0
tqdm>=4.65.0
pandas>=2.0.0

# Data upload / audio
moviepy>=1.0.3

# Notebooks / visualisation
jupyter>=1.0.0
ipywidgets>=8.0.0
matplotlib>=3.7.0

# SAM2 notebook still needs torch — kept here for that use case only
# (not needed by the VLA agent itself)
torch>=2.5.1
torchvision>=0.20.1
hydra-core>=1.3.2
